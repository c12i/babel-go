<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ .title }} - Library of Babel</title>
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg" />

    <script src="https://cdn.tailwindcss.com"></script>

    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              parchment: "#f4f1e8",
              ink: "#1a1a1a",
              aged: "#c9b99b",
            },
            fontFamily: {
              mono: ["Courier New", "Courier", "monospace"],
            },
          },
        },
      };
    </script>

    <style type="text/tailwindcss">
      @layer components {
        .location-link {
          @apply bg-transparent border-0 text-aged font-mono p-0 cursor-pointer text-left hover:text-amber-600 transition-colors w-full;
        }

        .page-content {
          @apply font-mono text-xs sm:text-sm leading-relaxed sm:leading-loose tracking-wide whitespace-pre break-words;
          display: block;
          margin: 0 auto;
          width: 100%;
          overflow-x: auto;
        }

        @media (min-width: 640px) {
          .page-content {
            width: fit-content;
          }
        }
      }

      body {
        background: linear-gradient(to bottom, #0a0a0a 0%, #1a1a1a 100%);
      }

      mark {
        padding: 2px 4px;
        border-radius: 2px;
      }
    </style>
  </head>
  <body class="min-h-screen text-parchment font-mono">
    <header class="border-b border-aged/20 py-3 sm:py-4 md:py-6">
      <div class="container mx-auto px-3 sm:px-4">
        <div class="flex items-center justify-between">
          <a
            href="/"
            class="text-lg sm:text-xl md:text-2xl tracking-widest text-aged hover:text-amber-600 transition-colors"
          >
            THE LIBRARY
          </a>
          <nav class="flex gap-4 sm:gap-6 md:gap-8 text-xs sm:text-sm tracking-wider">
            <a
              href="/search"
              class="text-aged/80 hover:text-aged transition-colors"
              >SEARCH</a
            >
            <a
              href="/browse"
              class="text-aged/80 hover:text-aged transition-colors"
              >BROWSE</a
            >
          </nav>
        </div>
      </div>
    </header>

    <main class="container mx-auto px-3 sm:px-4 py-4 sm:py-6 md:py-8">
      <div class="max-w-5xl mx-auto">
        {{ if .error }}
        <div
          class="bg-red-900/20 border border-red-900/50 text-red-400 p-4 mb-8 text-sm max-w-3xl mx-auto"
        >
          {{ .error }}
        </div>
        {{ end }}

        {{ if not .location }}
        <div class="max-w-3xl mx-auto">
          <div class="bg-ink/50 border border-aged/20 rounded p-4 sm:p-6 md:p-8">
            <h1 class="text-xs tracking-[0.3em] text-aged/60 uppercase mb-4 sm:mb-6">
              Navigate to Location
            </h1>

            <form action="/browse" method="POST" class="space-y-3 sm:space-y-4">
              <div>
                <label
                  for="location"
                  class="block text-aged/50 text-xs tracking-wider mb-2"
                >
                  HEXAGON.WALL.SHELF.BOOK.PAGE
                </label>
                <input
                  type="text"
                  name="location"
                  id="location"
                  placeholder="e.g., 3a7f.2.1.15.204"
                  class="w-full bg-ink/80 border border-aged/30 rounded px-3 py-2 sm:px-4 sm:py-3 text-parchment font-mono text-xs sm:text-sm focus:outline-none focus:border-aged/60 transition-colors placeholder-aged/30"
                  autofocus
                />
              </div>
              <button
                type="submit"
                class="w-full bg-aged/10 hover:bg-aged/20 border border-aged/30 text-aged px-4 py-2 sm:px-6 sm:py-3 rounded transition-all tracking-widest text-xs sm:text-sm uppercase"
              >
                Navigate
              </button>
            </form>
          </div>
        </div>
        {{ end }} {{ if .location }}
        <div class="space-y-3 sm:space-y-4 md:space-y-6">
          <div class="bg-ink/50 border border-aged/20 rounded p-3 sm:p-4 md:p-6">
            <div class="flex items-center justify-between mb-3 sm:mb-4">
              <p class="text-aged/40 text-xs tracking-widest uppercase">
                Location
              </p>
              <button
                onclick="copyLocation()"
                id="copyBtn"
                class="bg-aged/10 hover:bg-aged/20 border border-aged/30 text-aged px-2 sm:px-3 py-1 rounded transition-all text-xs tracking-wider uppercase"
              >
                Copy
              </button>
            </div>
            <p
              id="locationText"
              class="font-mono text-aged text-xs sm:text-sm mb-3 sm:mb-4 break-all truncate"
              title="{{ .location.String }}"
            >
              {{ .location.String }}
            </p>

            <div
              class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-5 gap-2 sm:gap-3 md:gap-4 text-xs pt-3 sm:pt-4 border-t border-aged/10"
            >
              <div>
                <span class="text-aged/40 block mb-1">Hexagon</span>
                <span
                  class="font-mono text-aged/80 truncate block max-w-[120px]"
                  title="{{ .location.Hexagon }}"
                  >{{ .location.Hexagon }}</span
                >
              </div>
              <div>
                <span class="text-aged/40 block mb-1">Wall</span>
                <span class="font-mono text-aged/80">{{ .location.Wall }}</span>
              </div>
              <div>
                <span class="text-aged/40 block mb-1">Shelf</span>
                <span class="font-mono text-aged/80"
                  >{{ .location.Shelf }}</span
                >
              </div>
              <div>
                <span class="text-aged/40 block mb-1">Book</span>
                <span class="font-mono text-aged/80">{{ .location.Book }}</span>
              </div>
              <div>
                <span class="text-aged/40 block mb-1">Page</span>
                <span class="font-mono text-aged/80">{{ .location.Page }}</span>
              </div>
            </div>
          </div>

          <div class="bg-ink/70 border border-aged/30 rounded p-3 sm:p-4 md:p-6 lg:p-8 shadow-2xl">
            <div class="mb-2 sm:mb-3 md:mb-4 pb-2 sm:pb-3 md:pb-4 border-b border-aged/20">
              <p class="text-aged/40 text-xs tracking-widest uppercase">
                Page {{ .location.Page }}
              </p>
            </div>

            <div class="bg-parchment/5 rounded p-2 sm:p-3 md:p-4 lg:p-6">
              <pre class="page-content text-parchment/90">
{{ .displayContent }}</pre
              >
            </div>
          </div>

          <div class="flex flex-col sm:flex-row justify-between items-center gap-2 sm:gap-0 pt-3 sm:pt-4">
            <a
              href="/search"
              class="text-aged/60 hover:text-aged transition-colors text-xs sm:text-sm tracking-wider"
            >
              ← Return to Search
            </a>

            <a
              href="/"
              class="text-aged/60 hover:text-aged transition-colors text-xs sm:text-sm tracking-wider"
            >
              Return to Library Entrance
            </a>
          </div>
        </div>
        {{ end }}
      </div>
    </main>

    <footer class="pb-4 sm:pb-6 md:pb-8 text-center text-aged/40 text-xs tracking-widest">
      <p class="mt-3 sm:mt-4 md:mt-6">
        <a
          href="https://github.com/c12i/babel-go"
          target="_blank"
          rel="noopener noreferrer"
          class="text-aged/60 hover:text-aged transition-colors"
        >
          View on GitHub
        </a>
      </p>
    </footer>

    <script>
      function copyLocation() {
        const locationText = document
          .getElementById("locationText")
          .textContent.trim();
        const copyBtn = document.getElementById("copyBtn");

        navigator.clipboard
          .writeText(locationText)
          .then(() => {
            // Show success feedback
            const originalText = copyBtn.textContent;
            copyBtn.textContent = "Copied!";
            copyBtn.classList.add(
              "bg-green-600/20",
              "border-green-600/50",
              "text-green-400",
            );
            copyBtn.classList.remove(
              "bg-aged/10",
              "border-aged/30",
              "text-aged",
            );

            // Reset after 2 seconds
            setTimeout(() => {
              copyBtn.textContent = originalText;
              copyBtn.classList.remove(
                "bg-green-600/20",
                "border-green-600/50",
                "text-green-400",
              );
              copyBtn.classList.add(
                "bg-aged/10",
                "border-aged/30",
                "text-aged",
              );
            }, 2000);
          })
          .catch((err) => {
            console.error("Failed to copy:", err);
            copyBtn.textContent = "Failed";
            setTimeout(() => {
              copyBtn.textContent = "Copy";
            }, 2000);
          });
      }
    </script>
  </body>
</html>
